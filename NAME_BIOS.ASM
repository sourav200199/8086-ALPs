ASSUME CS:CODE, ES:EXTRA
EXTRA SEGMENT
MSG DB 'SOURAV 20BCB0054'
EXTRA ENDS
CODE SEGMENT
START: MOV AX, EXTRA
       MOV ES, AX
       MOV AH, 13H ; SERVICE NO.
       MOV AL, 1   ; WRITE ATTR.
       MOV CX, 16H ; COUNTER
       MOV BH, 00H ; PAGE NO.
       MOV BL, 07H ; ATTRIBUTE - COLOR
       MOV DX, 0202H ; COL. AND ROW NO.
       LEA BP, MSG
       INT 10H
       MOV AX, 4C00H
       INT 21H
CODE ENDS
END START
END

